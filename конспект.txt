<?php
    echo "Hello world";
?>
Тело функции с выводом "Привет, мир!"

//   это
#    и это всё однострочный комментарий

/* 
        а это многострочный
        а это многострочный
        а это многострочный
*/

$ - Ставится перед всеми переменными
$mycounter = 5;

Массивы

    $myarray = array("One", "Two", "Three");
    echo $myarray[1];

    $oxo = array(array("x","o","x"), 
                array("o","o","x"), 
                array("x","o"," "));
    echo $oxo[0][1];


Инкремент 
$count = 2;
    $count += 1;
    echo $count;

Складывание двух строк
$bulletin = "Большие сиськи только тут!!!";
    $newflash = " Сисяндры тут!!";
    $bulletin .= $newflash;
    echo $bulletin;

Типы строк:
Если требуется присвоить переменной значение текстовой
строки, сохраняя ее точное содержимое, нужно воспользоваться одинарными
кавычками:
    $info = 'Предваряйте имена переменных символом $ как в данном примере:
    $variable';
    Получится:  Предваряйте имена переменных символом $ как в данном примере: $variable

В то же время, если требуется включить в состав строки значение переменной,
используется строка, заключенная в двойные кавычки
    Тут получится: Получится:  Предваряйте имена переменных символом $ как в данном примере: 15